{% extends 'block.html.twig' %}
{% block content %}
    <div class="bg" style="background:url({{ file_url(content.field_image['#items'].entity.uri.value) }}) no-repeat center; background-size:cover;">
        <div class="container">
            <h1>{{ content.field_title }}</h1>
        </div>
        <div class="scroll"><a href="#featured">Learn More</a></div>
    </div>
{% endblock %}